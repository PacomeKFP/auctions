import { PropTypes } from "prop-types";
import { useState } from "react";
import {
  Button,
  CloseButton,
  Form,
  ListGroup,
  ListGroupItem,
  Tab,
} from "react-bootstrap";
export default function LotAndParticipantTab({ name }) {
  const [email, setEmail] = useState("");

  return (
    
  );
}
LotAndParticipantTab.propTypes = {
  name: PropTypes.string,
};
